# Mac App Store screenshots

Capture the exact signed App Store candidate at 1440 x 900 with production data replaced by the seeded local workspace. Required filenames and captions live in `../submission.json`.

Do not add framed mockups, browser chrome, credentials, customer data, Direct-only controls, prices, or capabilities that are absent from the submitted build.
