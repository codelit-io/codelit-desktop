# App Review: local engine availability

## Status as of 2026-09-10

Version 0.1.2 build 17 remains rejected. Apple requested information under
Guideline 2.1 because no engine was available on a 15-inch M3 MacBook Air.
A factual explanation was sent through App Store Connect and the posted reply
was verified. Replacement version 0.1.2 build 18 was built, signed, uploaded,
and processed by Apple as VALID. It is assigned to Codelit Mac Internal in
TestFlight. App Review resubmission remains pending the publication stop in
`../RELEASE_QA.md`; the signed candidate matrix is not complete.

## Confirmed cause

The bundled model manifest records `releaseValidatedMemoryGiB: [32]` for both
Qwen 3 models. `model_manager.rs` checks exact memory-class membership in both
model discovery and download preparation for release builds. Consequently,
non-32 GiB machines cannot even download the small model despite its 8 GiB
minimum-memory declaration. Apple's first screenshot shows this restriction;
the second shows the resulting empty engine selector.

This is not resolved by asking the reviewer to open Intelligence settings.
Do not add hardware classes to the validation list without physical evidence.
Do not present optional user-owned API credentials as a fix for the advertised
no-account local experience.

## Verification performed

- Installed `/Applications/Codelit 2.app` reports bundle build 17.
- Settings > Intelligence > On this Mac shows the existing verified Qwen 3
  0.6B and 8B models on the current 32 GiB Mac.
- The existing Test bot was temporarily switched to the fixed Qwen 3 0.6B
  model. Prompt: `Write one short sentence explaining what a checklist is.`
- Actual response: `A checklist is a list of items or steps to follow in a
  specific task.` This was a local model run, not a mocked renderer result.
- This does not qualify a fresh download or a non-32 GiB review device.

## Replacement release checklist

- [x] Read the current rejection and both attached screenshots.
- [x] Trace the empty selector to release hardware admission, not authentication.
- [x] Verify an actual response from the reviewed installed build on available hardware.
- [x] Reply to Apple without claiming the current binary is corrected.
- [ ] Obtain a non-32 GiB Apple-silicon test environment, ideally an 8/16/24 GiB
  Mac matching the reviewer class.
- [ ] Validate the small model's download, integrity check, benchmark, real
  conversation, cancellation, offline reuse, and relaunch on that hardware.
- [x] Separate minimum-memory admission from physical QA history; retain model
  minimum memory, disk, integrity, and benchmark safety checks. Physical
  qualification remains a separate incomplete release requirement.
- [x] Make the no-ready-engine state lead directly to an actionable local setup
  path and hide unavailable App Store-only provider choices where appropriate.
- [ ] Correct model setup and hardware expectations in App Review notes and
  customer-facing metadata.
- [ ] Run regression checks and verify the exact signed replacement through
  TestFlight, including the previously fixed export flow.
- [ ] Upload/select the replacement, resubmit, and verify the resulting review status.

App Store Connect does not state the review device's exact RAM capacity. Do
not invent it or call current-host testing equivalent to testing that device.

## Build 18 implementation

The user requested broad MacBook compatibility after this diagnosis. Build 18
replaces exact RAM-class membership with the model's declared minimum RAM:
8 GiB for Quick local and 16 GiB for the optional 8B model. Unknown memory fails
closed. The recorded physical-validation list remains unchanged. File hashes,
disk requirements, and the required device-local benchmark still apply.

First setup chooses the smallest compatible model. The empty engine picker
opens Intelligence settings, and unsupported subscription/local providers no
longer clutter the App Store provider list. Regression tests cover admission at
8, 16, 18, 24, 32, 36, 48, 64, 96, 128, 256, and 512 GiB; these are injected
capacity values, not physical hardware test results.

Intel support is not delivered by this change: the existing package, helper,
release tooling, and MLX runtime are arm64-only. A universal app plus a separately
validated Intel-compatible inference backend remains necessary for Intel Macs.

## Build 18 delivery and remaining scope

- Source commit: `17c4d7067cbcd1ba895020ed1d1588647c1d8bcd` (clean, pushed).
- Apple build ID: `e1f359e3-92d5-4d8a-a7ed-ce51eab52536`, processed VALID.
- Package SHA-256: `92ee35f3dcb4677bd54bee75e0669baa616a487a3b0d6251fc6b47199db24ccf`.
- Evidence: `/Users/Shared/Codelit-App-Review-2026-09-10/build18-final/`.
- Tests: 330 JavaScript tests; native desktop check 265 passed, 7 ignored;
  7 App Store-feature model-manager tests passed. Source-bound renderer QA
  passed with 73 records and no reported console errors.
- Candidate receipt is `testflight-ready`, not release-qualified. All physical
  matrix results remain pending. The renderer uses native fixtures, not real
  model execution, and does not prove fresh hardware compatibility.
- TestFlight displays build 18. Installation was started and retried after
  stalling; installed build 18 and its native journey are not yet verified.
- App Store Connect still has build 17 selected; no resubmission was made.

For Intel support, implement and verify a separate Intel-capable inference
backend with pinned model files, the same integrity and local benchmark
requirements, and cancellation/resource limits. Produce universal main/helper
binaries, audit all bundled dependencies, and test signed App Store installs
on Intel and Apple silicon before widening the advertised compatibility.
Define the supported macOS floor explicitly; "any MacBook" cannot include
hardware unable to run that OS. Do not turn optional API keys into a substitute
for the promised free local experience.
