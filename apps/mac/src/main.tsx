import React from "react";
import ReactDOM from "react-dom/client";
import BotsApp from "./BotsApp";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <BotsApp />
  </React.StrictMode>,
);
